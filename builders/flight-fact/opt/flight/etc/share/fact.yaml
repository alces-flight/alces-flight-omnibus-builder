#==============================================================================
# This document contains the main configuration for flight-fact
# The full config specification can be found here:
# /opt/flight/opt/fact/etc/config.reference
#==============================================================================

# Uncomment the following line to connect to the staging server
# base_url: https://staging.center.alces-flight.com/api

# Setting the asset_id will put flight-fact into single asset mode
# The asset ID can be determined with following flight-asset (v1.1.2) command:
# $ flight asset show ASSET_NAME --verbose | cut -f 6

# Uncomment the following line to set the asset_id
# asset_id: ASSET_ID
